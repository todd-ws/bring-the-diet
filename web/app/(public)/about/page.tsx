import { Shell } from '@nutri/ui';

export default function Page() {
  return (
    <Shell title="about">
      <p style={{ maxWidth: 840 }}>
        Foundation screen placeholder. Next steps: connect to API, add list/detail routes, and implement filters, pagination, and auth gating.
      </p>
    </Shell>
  );
}
