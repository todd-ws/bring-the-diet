import { Shell } from '@nutri/ui';

export default function Page() {
  return (
    <Shell title="Comments">
      <p style={{ maxWidth: 900 }}>
        Foundation placeholder. Next steps: implement list to detail to edit workflows, plus moderation actions (approve/reject/delete).
      </p>
    </Shell>
  );
}
